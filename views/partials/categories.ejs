<% for(let one of categories){ %>
    <a href="http://localhost:3000/category/<%= one.category_name %>/<%= one._id %>"
        class="nav-item nav-link text-dark" id="<%= one._id %>">
        <%= one.category_name %>
    </a>
    <% } %>